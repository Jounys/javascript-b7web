let cores = ['Azul', 'Preto', 'Vermelho', 'Amarelo']
cores.push('Verde')

// for (let n = 0 ; n <= cores.length; n++) {
//     console.log(cores[n])
// }

for (let n9 in cores){ // forma diferente de fazer o for
    console.log(cores[n9])
}

// for (let cor of cores){ // Repete os valores do Array
//     console.log(cor)
// }